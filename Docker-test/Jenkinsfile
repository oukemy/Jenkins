pipeline {
    agent any

    stages {
        stage('Docker build') {
            steps {
                script {
                    sh "docker build -f Jenkins/Docker-test/Dockerfile -t oukemyjp/jenkinsbuildtest:1.0.0-${BUILD_ID} Jenkins/Docker_test"
                }
            }
        }
        stage('Docker push') {
            steps {
                scripts {
                    sh "docker push oukemyjp/jenkinsbuildtest:1.0.0-${BUILD_ID}"
                }
            }
        }
    }
}